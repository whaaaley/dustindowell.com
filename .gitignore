# Dependencies
node_modules/

# Build output
/client/dist/
/client/src/features/licenses/notices/
.vite/
.vite-ssg-temp/

# Env / secrets
.env
.env.*
!scripts/github/.env.template
!scripts/fly/.env.template
!client/.env.production
!client/.env.development

# Playwright / test artifacts
.auth/
/test-results/
/client/test-results/

# TypeScript build info
*.tsbuildinfo

# Backup bundles
*.bundle

# OS / editor
.DS_Store
*.log
scripts/**/.env

# Local-only files: Pencil design + fonts + notes, resume PDF exports, DNS notes, TODO, old site copy
/_local/

# Wrangler local state
.wrangler/
worker/worker-configuration.d.ts

# Symlink to the onclick repo's Claude rules (local only)
.claude/rules

# Temporary maintenance-page Worker used during the move; deleted after the production deploy
/_maintenance-worker/
